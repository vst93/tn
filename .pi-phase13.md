# Phase 13 — Quick Open + 笔记统计/阅读进度 + 滚动打磨

## 核心目标
在不膨胀功能的前提下加入一个高价值导航入口（Quick Open），扩展笔记统计与阅读进度反馈，并细化滚动手感与状态栏/内容面板的视觉层级。

## 不动的东西
- 配色体系（palette.go）完全保留：低饱和冷灰底 + 蓝色 accent
- 存储层保持普通 `.md` 文件，无新增依赖
- 不引入数据库或额外格式

## 新特性

### 1. Quick Open（Ctrl+O）
- 通过文件名/路径即时过滤所有笔记
- `↑/↓` 选择、`Enter` 打开并记录历史、`Esc` 取消
- 取消后恢复进入前的编辑/预览模式与焦点
- 命令面板新增 “Quick open”

### 2. 笔记统计与阅读进度
- 内容面板元数据行扩展为 `words · chars · lines · ~read time`
- 预览模式下按滚动位置显示 `NN% read`
- 状态栏空闲时显示 accent 进度条与 `NN%`，并继续保留 `★ pinned`

## UI 打磨
- 顶栏品牌与页签之间加入弱分隔，层级更清晰
- 内容面板标题在可滚动时显示阅读进度
- 状态栏合并进度与固定标记，避免挤压快捷键提示

## 交互优化
- 树滚动每次滚轮一格，预览/帮助滚轮步进收紧到 2 行
- 帮助面板、README 同步 `Ctrl+O` 与统计说明

## 验收
- `go build ./...` 通过
- `go test ./...` 通过（新增 Quick Open、统计、阅读进度、树滚动测试）
- `go vet ./...` 通过
- README 与帮助文本同步更新

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase13): quick open + note stats/reading progress + scroll polish"`
