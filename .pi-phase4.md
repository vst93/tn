# Phase 4 — 复制体验彻底重构 + 界面细节精炼

## 核心目标
解决复制功能的核心痛点（复制内容 ≠ 看到内容），同时精炼界面细节，让整体更专业、更美观。

## 当前问题

### 复制功能（最重要）
1. 预览模式下 `copyCurrent()` 复制的是 `m.editor.Value()`（原始 Markdown），但用户看到的是 glamour 渲染后的 ANSI 文本 — 复制内容 ≠ 看到内容
2. 没有"复制当前行"或"复制编辑器选区"的功能
3. OSC 52 回退可能静默失败，用户不知道复制是否成功、复制了什么
4. Ctrl+G 选择模式体验别扭 — 禁用鼠标、提示终端选择、按任意键恢复

### 界面细节
1. 树形视图的折叠/展开标记太粗糙（▾/▸/○）
2. 边框语言太重 — 到处都是 ┌─┐│└┘，非聚焦面板也用了明显边框
3. 状态栏的快捷键提示和状态消息混在一起
4. 编辑模式的视觉区分不够强（只有 header 变了）
5. 对话框/帮助框的边框可以更轻量

### 交互问题
1. 预览区滚轮行为可更顺滑
2. 窄屏下 tree 切换体验可优化

## 不动的东西
- 配色体系（palette.go 已定义好，直接使用变量）
- 布局结构（Phase 1 已落地的三段式宽度、左右结构不动）
- Markdown 渲染（Phase 2 已优化的 glamour 样式不动）
- main.go / storage 层
- 不引入新的外部依赖
- 所有快捷键语义保持不变（可以新增，不删改已有）

## 具体改动

### 一、复制功能重构（核心）

#### 1.1 预览模式复制渲染后的纯文本
- 新增 `extractPlainText(rendered string) string` 函数：从渲染后的 ANSI 文本中去除 ANSI 转义码，得到纯文本
- 在 preview 模式下，`copyCurrent()` 复制纯文本而非原始 Markdown
- 在 edit 模式下，继续复制原始 Markdown（因为用户编辑的就是 Markdown 源）
- 实现：复用已有的 `stripANSI()` 函数，加上去除其他控制字符的处理

#### 1.2 编辑器内复制增强
- 编辑模式下，如果 textarea 有选区（selection），复制选区内容而非全文
- 编辑模式下，如果没有选区，复制当前行（光标所在行）
- 新增快捷键 `Ctrl+L` = 复制当前行（在编辑模式下）
- 预览模式下，`Ctrl+C` / `Ctrl+Y` 继续复制全文（渲染后的纯文本）

#### 1.3 复制反馈增强
- 复制成功后，status 显示：`✓ Copied: <内容前40字符>...`（截断显示）
- 复制失败时，显示具体错误原因
- 如果内容太长，显示 `✓ Copied 120 chars` 而非全文

#### 1.4 更健壮的剪贴板处理
- 保留现有 `copyText()` 的双通道逻辑（系统剪贴板 → OSC 52）
- 如果两个通道都失败，status 行显示明确的错误：`Copy failed: <原因> · Select text manually and use terminal copy`
- 不要静默失败

### 二、界面细节精炼

#### 2.1 树形视图优化
- 文件夹折叠标记：`▸`（合拢）/ `▾`（展开）→ 保留但改用更精致的样式
- 文件标记：`○` → 改为 `·` 或去掉（用缩进和颜色区分即可）
- 选中行：保持 selection 背景，但加左侧 ▸ 指示器（accent 色）
- 文件夹名用 `bold + text` 色，文件名用 `muted` 色（已有，确认生效）

#### 2.2 边框轻量化
- 非聚焦面板的边框颜色从 `rule` 改为更淡的 `muted` 色
- 聚焦面板保持 `accent` 色边框
- 考虑在宽屏模式下，左树和右内容区之间用单竖线 `│` 分隔而非各自完整边框

#### 2.3 状态栏优化
- 左侧：快捷键提示（muted 色）
- 右侧：状态消息（根据 success/error 变色）
- 中间用弹性空格分隔（已有逻辑，确认生效并优化）
- 编辑模式下，左侧显示 `Ctrl+S 保存 · Esc 退出 · Ctrl+L 复制行`，右侧显示状态

#### 2.4 编辑模式视觉强化
- 编辑模式下，内容区背景从 `bg` 微变为 `surface` 色（微妙的区分）
- 编辑模式下，header 右侧显示 `EDIT` 标签（已有，确认样式精致）
- 光标行高亮更明显（已有 CursorLine 背景色，确认生效）

#### 2.5 对话框/帮助框轻量化
- 减少边框的厚重感：用 `lipgloss.RoundedBorder()` 或更轻的边框
- 增加内距（padding 从 1,2 改为 1,3）
- 背景用 `surface` 色，与主界面区分

### 三、交互优化

#### 3.1 预览区滚轮
- 确保滚轮在预览区顺滑滚动（已有 MouseWheelEnabled，确认生效）
- 滚轮速度适中（MouseWheelDelta = 3，确认生效）

#### 3.2 窄屏体验
- 窄屏（<60）模式下，Tab 切换 pane 时保持当前笔记的上下文
- 从 content 切回 tree 时，之前选中的笔记保持选中

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- 预览模式下 Ctrl+C 复制的是渲染后的纯文本（不是原始 Markdown）
- 编辑模式下有选区时复制选区，无选区时复制当前行
- 复制反馈显示内容摘要
- 树形视图更精致
- 边框更轻量
- 编辑模式视觉区分更明显

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase4): copy overhaul + UI refinement — plain-text copy in preview, line copy, elegant borders"`
- 不要 push
