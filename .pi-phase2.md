# Phase 2 — Markdown 综合渲染优化

## 目标
把当前基础 Markdown 渲染升级为更有层次、更专业、更美观的终端 Markdown 体验。

## 当前问题
- H1/H2/H3 视觉层次不清（都只是 bold，没有大小/颜色差异）
- Code block 缺少背景色区分，与正文混在一起
- Table 完全没有样式
- Blockquote 样式简陋
- `renderMarkdown()` 每次调用都重建 glamour renderer（性能浪费）
- 整体渲染效果仍偏"demo"

## 不动的东西
- 配色体系（palette.go 已定义好，直接使用变量）
- 布局结构（Phase 1 已落地的三段式宽度、左右结构不动）
- 快捷键语义、交互行为、存储逻辑
- main.go / storage 层
- 不引入新的外部依赖（继续用 glamour）

## 具体优化点

### 1. 标题层次（Heading Hierarchy）
- H1: 大写 + 加粗 + accent 颜色 + 下方分隔线
- H2: 加粗 + accent 颜色 + 前缀 `## ` 保留
- H3: 加粗 + text 颜色 + 前缀 `### ` 保留
- H4-H6: 加粗 + muted 颜色

### 2. 代码块（CodeBlock）
- 使用 surface 背景色填充整个代码块区域
- 行号用 muted 颜色
- 保留 chroma 语法高亮（glamour 已内置）
- 代码块顶部加一个细的边框线

### 3. 行内代码（Code）
- surface 背景 + text 前景
- 前后留一个空格的内距感

### 4. 引用块（BlockQuote）
- 左侧用 `│` 字符 + muted 颜色做缩进线
- 文字用 muted + italic
- 不要额外的边框

### 5. 表格（Table）
- glamour 默认支持表格渲染，确保 StyleConfig 里 Table 有合理配置
- 表头加粗 + accent
- 单元格间用 `│` 分隔
- 表格上下用 `─` 做边框

### 6. 列表（List）
- 无序列表用 `•`（已有）
- 有序列表用 `1.` `2.`（已有）
- 任务列表 `[✓]` `[ ]`（已有）
- 确保嵌套缩进清晰

### 7. 链接（Link）
- accent 颜色 + 下划线（已有，保留）

### 8. 水平线（HorizontalRule）
- 用 `────────` 全宽分隔线 + muted 颜色

### 9. 性能优化
- 缓存 glamour renderer，不要每次 renderMarkdown 都重建
- 只在 width 变化或 style 变化时重建

### 10. 段落间距
- 段落间保留一行空白（Document.Margin 已设 1，确认生效）

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- Markdown 预览区：标题有层次、代码块有背景、表格可读、整体精致
- 编辑模式不受影响

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`（proxy.golang.org 在本机超时）
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase2): markdown rendering overhaul — headings, code blocks, tables, quotes"`
- 不要 push
