# Phase 12 — 固定笔记 + 阅读时长 + 历史导航 + UI 打磨

## 核心目标
在不膨胀功能的前提下，加入三个高价值小特性，同时优化视觉层级、选中反馈、空状态、快捷键冲突、复制反馈与全局搜索上下文。

## 不动的东西
- 配色体系（palette.go）完全保留：低饱和冷灰底 + 蓝色 accent
- 存储层保持普通 `.md` 文件，无新增依赖
- 不引入数据库或额外格式

## 新特性

### 1. 固定笔记（Pin）
- 快捷键 `*` 切换选中笔记的 `pinned: true` 前置元数据
- 固定笔记在树内同级置顶，行首显示 `★`
- 内容面板标题和底栏状态显示固定标记
- 保存/自动保存/标签编辑后保持 pinned 状态同步

### 2. 阅读时长估算
- 内容面板元数据行显示 `N words · ~M min read`
- 短文本显示 `<1 min`，按 200 wpm 估算，并计入 CJK 字符
- 保留冷灰蓝色视觉风格

### 3. 笔记历史导航
- 打开笔记时记录历史栈（去重、截断到 100 条）
- `Alt+←` 后退、`Alt+→` 前进
- 全局搜索打开、树打开共用同一 openPath 逻辑并记录历史

## UI 打磨
- 树选中行：`▸` 前缀 + 背景高亮 + 加粗，反馈更清晰
- 树/内容空状态给出更明确的操作提示
- 顶栏品牌使用 accent 色，层级更明确
- 底栏空闲时显示 `★ pinned` 状态
- 帮助面板高度微调，确保新条目可见

## 交互优化
- 修复 `g` 快捷键冲突：单键 `g` 恢复为树内跳转到顶部，选择终端文本改由 `Ctrl+G`
- 复制反馈更明确：短文本显示内容、中长文本显示字符数、超长文本显示字符数与词数
- 全局搜索结果增加行号与匹配上下文窗口（`…` 截断）

## 验收
- `go build ./...` 通过
- `go test ./...` 通过（新增 pin、历史、g 冲突、阅读时长、搜索上下文测试）
- `go vet ./...` 通过
- README 与帮助文本同步更新

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase12): pin notes + reading time + back/forward history + UI polish"`
