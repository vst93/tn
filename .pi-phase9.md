# Phase 9 — 一致性打磨 + 回归修复 + 批量操作

## 核心目标
让软件更精致：修复已知问题、统一视觉语言、补全批量操作。

## 不动的东西
- 配色体系（palette.go）
- 主入口 / 存储层
- Phase 1-8 的所有功能
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 回归修复

#### 1.1 Ctrl+C 行为统一
- 预览模式 `Ctrl+C`：复制渲染后的纯文本（Phase 4 已实现，确认）
- 编辑模式 `Ctrl+C`：有选区复制选区，无选区复制当前行（Phase 4 已实现，确认）
- 编辑模式下 `Ctrl+C` 没有选区且行为应该与预览模式一致（复制当前行）

#### 1.2 搜索高亮性能
- 如果搜索结果很多（100+ 匹配），高亮渲染可能卡顿
- 优化：只高亮当前可见区域的匹配项，或限制高亮数量

#### 1.3 导出路径
- 如果用户输入绝对路径，直接保存
- 如果相对路径，基于当前笔记目录解析
- 路径不存在时创建目录

### 2. 视觉一致性

#### 2.1 边框语言统一
- 聚焦面板：`accent` 色边框
- 非聚焦面板：`muted` 色边框（暗灰）
- 对话框/帮助：`RoundedBorder` + `muted` 边框
- 所有面板的标题都用 `muted`（非聚焦）或 `accent`（聚焦）

#### 2.2 底栏统一
- 普通模式：左侧快捷键 + 右侧状态消息
- 编辑模式：左侧 `Ctrl+S · Ctrl+Z · Ctrl+Shift+Z · Esc · Ctrl+L` + 右侧行号
- 搜索模式：左侧搜索提示 + 右侧匹配数
- 专注模式：只留极小 `Esc 退出专注`

#### 2.3 编辑区光标行
- 确认 CursorLine 背景用 `selection` 色
- 确认 LineNumber 用 `muted` 色
- 编辑区背景在编辑模式用 `surface`，预览模式用 `bg`

### 3. 批量操作

#### 3.1 多选笔记
- 在树形视图中：
  - `Space` 切换当前笔记的选中状态（多选）
  - 选中笔记前显示 `☑` 标记，未选中显示 `○`
  - `Ctrl+A` 全选当前可见笔记
  - `Ctrl+Shift+A` 取消全选
- 选中多个笔记后可执行：
  - 批量删除（确认弹窗）
  - 批量导出（依次保存到指定目录）
  - 批量打标签

#### 3.2 实现思路
- 新增 `selectedItems map[string]bool` 到 Model
- 树的 `activateSelected()` 逻辑改为：如果是目录仍展开/折叠，如果是文件则切换选中状态
- `Space` 键绑定到 toggleSelect()
- 确认弹窗要显示"将删除 N 个笔记"
- 批量导出时依次写入指定目录

### 4. 目录排序优化

- 在 `rebuildFlat()` 中：
  - 默认：文件夹在前，文件在后，各自按名称排序
  - 名称排序时忽略大小写（已实现）
- 新增排序选项（暂不做 UI，留作后续）：
  - 按修改时间排序
  - 按创建时间排序

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- `Space` 可多选笔记
- 多选后可批量删除
- 多选后可批量导出
- 多选后可批量打标签
- 视觉边框统一、底栏统一
- 编辑区光标行可见

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase9): batch ops + visual polish + regression fixes"`
- 完成后 push 到远端
