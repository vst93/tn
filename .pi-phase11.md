# Phase 11 — 最终打磨 + 回归验证 + 文档更新

## 核心目标
统一视觉语言、修复所有已知问题、完善文档和帮助文本，交付一个完整的终端笔记应用。

## 不动的东西
- 配色体系（palette.go）
- 主入口 / 存储层
- Phase 1-10 的所有功能
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 视觉一致性最终审查

#### 边框语言
- 所有面板：聚焦 `accent` 色，非聚焦 `muted` 色
- 对话框/帮助：`RoundedBorder` + `muted` 边框
- 确认 `borderedPanelPart` 在所有调用点正确传递 `leftB`/`rightB`

#### 底栏统一
- 普通模式：左侧快捷键（muted）+ 右侧状态（根据 success/error 变色）
- 编辑模式：左侧 `Ctrl+S · Ctrl+Z · Ctrl+Shift+Z · Esc · Ctrl+L` + 右侧 `Ln X/Y · Col Z`
- 搜索模式：左侧搜索提示 + 右侧 `N/M matches`
- 专注模式：只留 `Esc 退出专注`

#### 编辑区
- 确认编辑模式背景 = `surface`，预览模式背景 = `bg`
- 确认光标行高亮 = `selection`
- 确认行号 = `muted`

### 2. 回归修复

#### 已知问题
- `extractPlainText` 已修复（保留换行）
- `Ctrl+C` 行为已统一
- 搜索高亮已限制 200 处

#### 新增检查
- 标签过滤后新建笔记：新笔记应出现在过滤列表中（如果匹配标签）
- 专注模式下无法打开其他笔记（应该可以）
- 会话恢复后树展开状态正确
- 编辑器 undo/redo 后光标位置正确

### 3. 帮助文本更新

#### 当前帮助内容需要包含：
- 导航：`↑/↓ J/K`、`←/→ H/L`、`Enter`、`Tab`
- 笔记：`Ctrl+N`（新建+模板）、`Ctrl+D`（文件夹）、`F2/R`（重命名）、`Delete/X`（删除）
- 编辑：`Ctrl+E`（切换编辑/预览）、`Ctrl+S`（保存）、`Ctrl+Z`（撤销）、`Ctrl+Shift+Z/Ctrl+Y`（重做）
- 复制：`Ctrl+C/Ctrl+Y`（复制）、`Ctrl+L`（复制当前行）
- 搜索：`Ctrl+F`（笔记内搜索）、`Ctrl+Shift+O`（全局搜索）、`Alt+G`（跳转到行）
- 导航：`Ctrl+Shift+F`（专注模式）、`Ctrl+Shift+P`（命令面板）
- 标签：`Ctrl+Shift+T`（编辑标签）、`#`（过滤标签）
- 批量：`Space`（多选）、`Ctrl+A`（全选）、`Ctrl+Shift+A`（取消全选）
- 导出：`Ctrl+Shift+E`（导出）
- 应用：`Ctrl+Q`（退出）、`?`（帮助）、`Esc`（取消/关闭）

### 4. 文档更新

#### README.md 更新
- Features 列表更新（包含所有 Phase 1-10 的功能）
- Controls 表格更新（包含所有快捷键）
- 添加截图/演示说明

#### 帮助命令文本优化
- 分组更清晰
- 每行对齐
- 搜索功能（在帮助中输入过滤）

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- 所有功能正常工作
- 视觉一致
- 文档更新

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase11): final polish + regression verification + docs update"`
- 完成后 push 到远端
