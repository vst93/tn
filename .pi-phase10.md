# Phase 10 — 自动保存 + 命令面板 + 快捷键提示浮层

## 核心目标
让笔记更安心（自动保存）、操作更高效（命令面板）、快捷键更直观（提示浮层）。

## 不动的东西
- 配色体系（palette.go）
- 主入口 / 存储层 / 主流程
- Phase 1-9 的所有功能
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 自动保存

#### 交互流程
- 编辑模式下，用户停止输入 2 秒后自动保存
- 保存成功后不显示任何提示（静默），避免打扰写作
- 只在保存失败时显示错误（红色）
- 手动保存（Ctrl+S）仍然可用，立即保存

#### 实现思路
- 在 Model 中新增 `lastEditTime time.Time` 字段
- 每次编辑操作更新 `lasteditTime = time.Now()`
- 在 Update 中检查：如果当前时间 - lastEditTime > 2s 且 dirty，自动保存
- 使用 tea.Tick 或类似机制检查
- 保存失败时设置 `statusErr = true` 并显示错误

#### 代码逻辑
```go
case tea.KeyMsg:
    if m.mode == modeEdit {
        m.lastEditTime = time.Now()
        // ... existing edit handling ...
    }

// In a separate tick command:
func autoSaveTick() tea.Msg { return autoSaveMsg{} }

case autoSaveMsg:
    if m.dirty() && time.Since(m.lastEditTime) > 2*time.Second {
        m.save() // silent on success
    }
```

### 2. 命令面板（Command Palette）

#### 交互流程
- `Ctrl+Shift+P` 打开命令面板
- 底部出现输入框，输入过滤命令
- 可用命令列表：
  - `Save note` — 保存当前笔记
  - `Undo` — 撤销
  - `Redo` — 重做
  - `Toggle edit/preview` — 切换编辑/预览
  - `Find in note` — 笔记内搜索
  - `Find everywhere` — 全局搜索
  - `Go to line` — 跳转到行
  - `Focus mode` — 专注模式
  - `Export note` — 导出笔记
  - `New note` — 新建笔记
  - `New folder` — 新建文件夹
  - `Rename` — 重命名
  - `Delete` — 删除
  - `Toggle tag filter` — 切换标签过滤
- 实时过滤命令列表
- `↑/↓` 选择，`Enter` 执行
- `Esc` 关闭

#### 实现思路
- 新增 `modeCommand` mode
- 用 `textinput.Model` 接收输入
- 命令列表用 `[]Command` 存储，每个命令有 `Name` 和 `Action func()`
- 过滤：`strings.Contains(strings.ToLower(cmd.Name), strings.ToLower(query))`
- 执行：调用对应的 `Action` 函数

### 3. 快捷键提示浮层

#### 交互流程
- `?` 打开帮助（已有）
- 改为更直观的浮层：
  - 快捷键按功能分组
  - 每行：左侧快捷键（accent 色）+ 右侧说明（muted 色）
  - 支持滚轮滚动
- `Esc` 关闭

#### 实现
- 当前帮助已经是分组展示，保持不变
- 优化：在帮助浮层中增加搜索功能（输入过滤快捷键）

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- 编辑停止 2 秒后自动保存（静默）
- 保存失败显示错误
- `Ctrl+Shift+P` 打开命令面板
- 命令面板可过滤、选择、执行
- 帮助浮层可用滚轮滚动

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase10): auto-save + command palette + shortcut cheat sheet"`
- 完成后 push 到远端
