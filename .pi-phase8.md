# Phase 8 — 标签系统 + 笔记模板

## 核心目标
让笔记有条理：支持标签分类、快速过滤、新建时选模板。

## 不动的东西
- 配色体系（palette.go）
- 主入口 / 存储层 / 主流程
- Phase 1-7 的所有功能
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 标签系统

#### 交互流程
- 笔记文件中用 front-matter 存储标签：
  ```markdown
  ---
  tags: [work, meeting]
  ---
  ```
- 或者简单地在笔记第一行后用 `#tag1 #tag2` 标记
- 选择用 **front-matter** 方案，更标准、更可控

#### 数据格式
- 在笔记 Markdown 文件开头用 YAML front-matter：
  ```markdown
  ---
  title: 会议记录
  tags: [work, meeting]
  created: 2026-08-14
  ---
  
  # 今日会议
  ...
  ```
- 如果笔记没有 front-matter，则 tags 为空

#### UI 展示
- 在笔记元数据行（编辑/预览标题下方）显示标签：
  - 每个标签用 `accent` 色 + 小方框 `▍ tag `
  - 标签太多时换行显示
- 在树形视图中，文件名后面显示标签数量标记：`○ meeting-notes #2`（表示 2 个标签）

#### 标签管理
- 编辑模式下 `Ctrl+Shift+T` 打开标签编辑对话框
- 输入框显示现有标签，逗号分隔
- Enter 保存标签到笔记 front-matter
- `Esc` 取消

#### 标签过滤
- 底栏左侧新增 `#` 过滤按钮
- 点击后底部出现标签输入框
- 输入标签名，树形视图只显示包含该标签的笔记
- `Esc` 清除过滤，显示全部

#### 实现思路
- 新增 `parseFrontMatter(content string) (meta FrontMatter, body string)` 函数
- `type FrontMatter struct { Title string; Tags []string; Created string }`
- 新增 `writeFrontMatter(body string, meta FrontMatter) string` 函数
- 标签过滤：在 `rebuildFlat()` 中根据当前过滤标签过滤笔记
- 新增 `tagFilter string` 字段到 Model

### 2. 笔记模板

#### 交互流程
- `Ctrl+N` 新建笔记时，弹出模板选择：
  1. **空白笔记** — 只有 `# Title`
  2. **日报** — 包含 `## 今日完成`、`## 明日计划`、`## 问题/阻塞`
  3. **会议记录** — 包含 `## 参会人`、`## 议题`、`## 结论`、`## 待办`
  4. **读书笔记** — 包含 `## 书名`、`## 作者`、`## 核心观点`、`## 摘录`、`## 我的思考`
- 选择后编辑器中预填模板内容，用户直接编辑

#### 模板定义
- 在代码中硬编码模板（不需要配置文件）
- 用 map 存储：
  ```go
  var noteTemplates = map[string]string{
      "blank": "# {{title}}\n\n",
      "daily": "## 今日完成\n\n\n## 明日计划\n\n\n## 问题/阻塞\n\n",
      "meeting": "## 参会人\n\n\n## 议题\n\n\n## 结论\n\n\n## 待办\n\n",
      "book": "## 书名\n\n\n## 作者\n\n\n## 核心观点\n\n\n## 摘录\n\n\n## 我的思考\n\n",
  }
  ```

#### 实现思路
- 新增 `modeTemplate` mode
- 底部显示模板选择列表：
  ```
  选择模板:
    1. 空白笔记
    2. 日报
    3. 会议记录
    4. 读书笔记
  ```
- 数字键选择，Enter 确认，Esc 取消
- 选择后创建笔记文件，写入模板内容，进入编辑模式

### 3. 标签在树中的分组视图

#### 交互流程
- 树形视图新增分组模式：
  - 默认模式：按文件夹分组（已有）
  - `# Tags` 模式：按标签分组，笔记按标签归类显示
- `Ctrl+T` 切换分组模式

#### 实现
- 当前不做，留作后续
- 在规格中记录为"可选"，pi 可根据实现难度决定

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- 笔记 front-matter 正确解析和保存
- 编辑模式下 `Ctrl+Shift+T` 可编辑标签
- 预览/编辑时标签显示在元数据行
- 树中显示标签数量
- 底栏 `#` 可过滤标签
- `Ctrl+N` 可选择笔记模板
- 模板内容正确预填

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase8): tag system + note templates — front-matter tags, tag filter, template selector"`
- 完成后 push 到远端
