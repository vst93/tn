# Phase 1 — 配色体系 + 尺寸自适应骨架 + Details 简化

## 当前状态
- `internal/app/palette.go` 已被之前的 subagent 新建，定义了新配色常量
- `internal/app/app.go` L92-L110 里还有旧的颜色变量声明（与 palette.go 重复）
- 目前编译失败：变量重复声明（bg/surface/selection/text/muted/rule/accent/green/warning/danger）

## 目标
把 `app.go` 里的视觉层（配色常量、面板渲染、布局组织、Details 面板、状态/底栏）调整为更现代、简洁、专业美观的风格。

## 不动的东西（必须保留）
- 所有快捷键语义、交互行为、存储逻辑、树/编辑/预览的数据流不动
- `store` 层、`main.go` 入口不动
- 功能列表（新建/重命名/删除/保存/复制/刷新/帮助/切换 pane 等）保持完整

## 验收方式
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过

## 约束与风格方向
- 采用 palette.go 已定义的低饱和冷灰底 + 柔和蓝 accent 配色（已在 palette.go 中定义好）
- 把 app.go 里的重复声明删除，改为引用 palette.go 中的变量
- 尽量减少硬边框套叠；聚焦的面板才用明显边界，非聚焦区域用更柔和的分隔/留白
- 整体保留左右结构，但在不同尺寸下表现不同：
  - 宽度足够（>=100）：左右共存，左树合理宽度（约 30-35）
  - 中等（60-99）：左右共存但左树偏窄（约 20-25）
  - 太窄（<60）：只保留一个 panel，通过 Tab 切换 tree / edit / preview
- 当用户切换到右侧（内容/编辑/预览）时，左侧树不应干脆消失，而是缩小成窄栏
- Details 面板不再单独成框。状态相关元信息降级为右侧内容区顶部的一条细信息栏或编辑区标题行里的元数据
- 底栏/状态行的组织更清楚：快捷键提示与状态消息不混在一起吵

## 工作范围（仅视觉/布局层）
1. 修复编译错误：删除 app.go L92-L110 的重复颜色声明，改为使用 palette.go 中已定义的配色
2. 调整 `headerView` / `treeView` / `contentView` / `statusView` / `dialogView` / `helpView` 的视觉组织
3. 重构宽度响应逻辑 (`resize`)，加入窄/中/宽三段表现
4. 删除/简化现有单独 Details 面板
5. 统一对话框/帮助框的浮层风格

## 输出要求
- 不要引入新的外部依赖
- 不要更改程序启动、flag、store 初始化、主入口行为
- 保留已有测试通过
- 构建用代理：`http_proxy=http://127.0.0.1:7890`
- `go env GOPROXY` 可能需要设为 `https://goproxy.cn,direct`（proxy.golang.org 在本机超时）

## 完成信号
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- Details 不再独占模块
- 整体不再是"demo 套边框"的感觉
