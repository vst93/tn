# Phase 5 — 笔记内搜索 + 导航增强 + 状态栏信息

## 核心目标
让长笔记不再难浏览：支持搜索、跳转、实时位置感知。同时补全状态栏的专业信息展示。

## 不动的东西
- 配色体系（palette.go）
- 布局结构（三段式宽度、左右结构）
- Markdown 渲染（glamour 样式）
- 复制功能（Phase 4 刚做完）
- 主入口 / 存储层 / 主流程
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 笔记内搜索（最重要）

#### 交互流程
- `Ctrl+F` 触发搜索模式：底部出现搜索栏（类似 Vim `/`），光标聚焦到输入框
- 输入时实时高亮预览区中所有匹配文本（用反色 `Reverse(true)` 或黄色背景）
- `Enter` / `n` 跳到下一个匹配，`Shift+Enter` / `N` 跳到上一个
- `Esc` 退出搜索模式，清除高亮，焦点返回预览区
- 搜索模式下底栏显示 `2/8 matches` 这样的当前位置

#### 实现思路
- 新增 `modeSearch` mode
- 输入框用现有的 `textinput.Model`
- 搜索逻辑：在 `renderedPlain`（已去 ANSI 的纯文本）中找所有匹配位置
- 高亮渲染：在 preview 内容中找到匹配文本，用 `lipgloss.NewStyle().Reverse(true).Background(warning)` 包裹
- 导航：维护 `searchMatches []matchPos` 和 `searchIndex int`，按 index 滚动 viewport 使匹配可见
- 滚动：用 `m.preview.GotoLine(line)` 或计算 offset 使匹配行居中

#### 约束
- 搜索不区分大小写（更友好的默认行为）
- 如果无匹配，底栏显示 `No matches` 用 danger 色
- 搜索框支持普通 Esc 取消、Enter 确认后继续按 Enter 跳下一个
- 搜索高亮只在预览模式有效（编辑模式不需要，因为有原生查找）

### 2. 跳转到行

#### 交互流程
- `Alt+G`（或 `Ctrl+G` 改绑定，因为 `Ctrl+G` 当前是 select mode）
- 底部出现输入框，提示 `Go to line:`
- 输入数字后 Enter，预览区/编辑区跳到对应行
- `Esc` 取消
- 如果行号超出范围，跳到最后一行并提示

#### 实现
- 复用 `startPrompt` 的模式，新增 `promptGotoLine` 类型
- 输入框限制为数字
- 预览模式：设置 `m.preview.Offset = line - someVisibleLines`
- 编辑模式：`m.editor.SetCursor(row-1)` 或通过 KeyMsg 发 End 再发 Up

### 3. 状态栏信息增强

#### 编辑模式底栏
- 左侧：`Ctrl+S 保存 · Esc 退出 · Ctrl+L 复制行`
- 右侧：`Ln 12 / 87 · Col 3` 显示当前行号/总行号 + 列号

#### 预览模式底栏（无 note 打开时保持原样，有 note 时）
- 左侧：快捷键提示
- 右侧：`128 words · 87 lines · ~1 min read`

#### 搜索模式底栏
- 左侧：`Next: Enter · Prev: Shift+Enter · Close: Esc`
- 右侧：`3 / 12 matches`

### 4. 编辑模式：回车自动缩进

当用户在一行末尾按 Enter 时：
- 如果上一行有缩进（空格/Tab），新行继承相同缩进
- 如果上一行以 `- `、`1. `、`[ ] `、`[x] ` 开头，新行继续列表

这个对 Markdown 编辑很实用，但不做太复杂，先做缩进继承和列表续行。

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- Ctrl+F 能搜索预览文本，实时高亮，Enter 跳下一个
- Alt+G 能跳转到指定行
- 编辑模式底栏显示行号/总行号
- 预览模式底栏显示字数/行数/阅读时间
- 编辑时回车自动继承缩进和列表

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase5): in-note search, goto line, status bar info, auto-indent"`
- 完成后 push 到远端
