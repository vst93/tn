# Phase 6 — 撤销/重做 + 导出 Markdown

## 核心目标
补齐写作体验的最后一环：undo/redo + 文件导出。

## 不动的东西
- 配色体系（palette.go）
- 布局结构（三段式宽度、左右结构）
- Markdown 渲染（glamour 样式）
- 复制 / 搜索 / 跳转 / 自动缩进（Phase 4-5）
- 主入口 / 存储层 / 主流程
- 不引入新的外部依赖（用 bubbles textarea 内置的 undo/redo）
- 快捷键不删改，只新增

## 具体改动

### 1. 撤销/重做（最重要）

#### 交互流程
- `Ctrl+Z` 撤销上一步编辑操作
- `Ctrl+Shift+Z` / `Ctrl+Y` 重做
- 操作保存后清空重做栈（不可撤销"保存"，这符合用户预期）

#### 实现思路
- `bubbles/textarea` 内置 `Undo()` 和 `Redo()` 方法
- 在 `Model` 中跟踪 undo/redo 可用性：
  ```go
  type Model struct {
      // ... existing fields ...
      undoStack []string  // Could use built-in
      redoStack []string
  }
  ```
- 利用 `textarea.Model` 的 `Undoable()` / `Redoable()` 方法判断是否可撤销/重做
- 底栏显示 `Ctrl+Z 撤销` 时根据可用性用 muted（不可用）或 accent（可用）着色

#### 关键实现
```go
case "ctrl+z":
    if m.mode == modeEdit {
        m.editor.Undo()
        return true, false
    }
    return false, false
case "ctrl+shift+z":
    if m.mode == modeEdit {
        m.editor.Redo()
        return true, false
    }
    return false, false
```

#### 底栏更新
- 编辑模式底栏左：`Ctrl+S 保存 · Ctrl+Z 撤销 · Ctrl+Shift+Z 重做 · Esc 退出 · Ctrl+L 复制行`
- 如果 undo/redo 不可用，对应的快捷键提示用更淡的 muted 色

### 2. 导出 Markdown

#### 交互流程
- `Ctrl+E` 在编辑模式 = 切换预览（已有）
- 新增菜单式导出：在 preview 模式下 `Ctrl+Shift+E` 触发导出对话框
- 导出选项：
  1. **复制到剪贴板**（用现有 copyCurrent 逻辑）
  2. **另存为** — 弹出输入框，输入文件名/路径，保存到本地
- `Esc` 取消导出

#### 实现思路
- 新增 `modeExport` mode
- 用 `textinput.Model` 接收导出路径
- 导出逻辑：
  - 复制到剪贴板：`copyText(m.editor.Value())`（预览模式用 renderedPlain）
  - 另存为：`os.WriteFile(path, []byte(m.editor.Value()), 0o644)`
- 导出成功后 status 显示 `✓ Exported to <path>` 或 `✓ Copied to clipboard`

#### 路径处理
- 默认路径 = 当前笔记所在目录 + 笔记名（去掉 `.md` 加 `.md` 或 `.html`）
- 如果用户输入的是相对路径，基于当前笔记目录解析
- 不强制 HTML 导出（太复杂），只导出 .md
- 路径非法时显示错误，不退出导出模式，让用户修正

### 3. 编辑模式：更好的光标行可见性

当光标在编辑区滚动时：
- 确保光标行始终可见（textarea 自带，确认生效）
- 光标行背景用 `selection` 色（已有，确认生效）

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- `Ctrl+Z` / `Ctrl+Shift+Z` / `Ctrl+Y` 在编辑模式下可撤销/重做
- 保存后重做栈被清空
- `Ctrl+Shift+E` 弹出导出对话框
- 导出可选择"复制到剪贴板"或"另存为"
- 另存为成功显示路径
- 编辑底栏显示撤销/重做快捷键提示

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase6): undo/redo + export dialog — Ctrl+Z/Shift+Z, Ctrl+Shift+E export"`
- 完成后 push 到远端
