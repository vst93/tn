name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag (no v prefix, e.g. 0.0.1)"
        required: true
        type: string
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: true

      - name: Build all platforms
        env:
          CGO_ENABLED: 0
        run: |
          VERSION="${{ github.event.inputs.version }}"
          LDFLAGS="-s -w -X main.version=${VERSION}"
          mkdir -p raw
          GOOS=linux   GOARCH=amd64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-linux-amd64       .
          GOOS=linux   GOARCH=arm64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-linux-arm64       .
          GOOS=darwin  GOARCH=amd64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-darwin-amd64      .
          GOOS=darwin  GOARCH=arm64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-darwin-arm64      .
          GOOS=windows GOARCH=amd64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-windows-amd64.exe .
          GOOS=windows GOARCH=arm64 go build -ldflags "$LDFLAGS" -o raw/tn-${VERSION}-windows-arm64.exe .

      - name: Package all platforms
        run: |
          VERSION="${{ github.event.inputs.version }}"
          mkdir -p dist
          for f in tn-${VERSION}-linux-amd64 tn-${VERSION}-linux-arm64 tn-${VERSION}-darwin-amd64 tn-${VERSION}-darwin-arm64 tn-${VERSION}-windows-amd64.exe tn-${VERSION}-windows-arm64.exe; do
            if [ -f "raw/${f}" ]; then
              # Use bare binary name inside archive
              suffix="${f#tn-${VERSION}-}"
              case "$f" in
                *.exe)
                  cp "raw/${f}" "raw/tn.exe"
                  zip "dist/tn-${VERSION}-${suffix%.exe}.zip" "raw/tn.exe"
                  rm "raw/tn.exe"
                  ;;
                *)
                  cp "raw/${f}" "raw/tn"
                  tar czf "dist/tn-${VERSION}-${suffix}.tar.gz" -C raw "tn"
                  rm "raw/tn"
                  ;;
              esac
            fi
          done
          rm -rf raw
          ls -lh dist/

      - name: Generate checksums
        run: |
          cd dist
          sha256sum tn-* > checksums.txt
          cat checksums.txt

      - name: Generate release notes
        id: notes
        run: |
          PREV=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -n "$PREV" ]; then
            LOG=$(git log --oneline --no-decorate ${PREV}..HEAD)
          else
            LOG=$(git log --oneline --no-decorate)
          fi
          echo "log<<EOF" >> "$GITHUB_OUTPUT"
          echo "$LOG" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: ${{ github.event.inputs.version }}
          prerelease: ${{ github.event.inputs.prerelease }}
          body: |
            ## What's Changed

            ${{ steps.notes.outputs.log }}

            ---

            ## 更新内容

            ${{ steps.notes.outputs.log }}

            ## Install

            **Install script:**

            ```bash
            curl -fsSL https://raw.githubusercontent.com/vst93/tn/main/cmd/install.sh | bash
            ```

            Options: `--install-dir <dir>`, `--force`, `--skip-github`, `--lang <en|zh>`

            **go install:**

            ```sh
            go install github.com/vst93/tn@${{ github.event.inputs.version }}
            ```

            Or download the binary for your platform from the assets below.
          files: dist/*
