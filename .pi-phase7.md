# Phase 7 — 专注模式 + 会话持久化 + 全局搜索

## 核心目标
让写作体验更沉浸，同时补全"下次打开时恢复原状"的专业感。

## 不动的东西
- 配色体系（palette.go）
- 主入口 / 存储层 / 主流程
- Phase 1-6 的所有功能
- 不引入新的外部依赖
- 快捷键不删改，只新增

## 具体改动

### 1. 专注模式（最重要）

#### 交互流程
- `Ctrl+Shift+F` 进入专注模式
- 专注模式下：
  - 隐藏树栏、隐藏标题栏、隐藏底栏
  - 整个屏幕只留编辑器/预览，最大可用空间
  - 顶部只留一个极小的 `≡` 或标题提示（半透明）
  - 底部只留一个极小的 `Esc 退出专注` 提示
- `Esc` 退出专注模式，恢复原样
- 专注模式下仍可切换编辑/预览、保存、复制

#### 实现思路
- 新增 `modeFocus` mode（或在 Model 中加 `focusing bool`）
- View() 中根据 focusing 状态决定是否渲染 header/tree/status
- 编辑器/预览区扩展到全屏尺寸
- 专注模式下的编辑区背景用更深的 bg（或保持 bg），营造沉浸感

#### 尺寸计算
```go
if m.focusing {
    m.editor.SetWidth(m.width - 4)  // 全屏宽度
    m.editor.SetHeight(m.height - 2) // 减去顶部提示和底部提示
    m.preview.Width = m.width - 4
    m.preview.Height = m.height - 2
}
```

### 2. 会话持久化

#### 交互流程
- 退出时自动保存当前状态：
  - 当前打开的笔记路径
  - 光标位置（行号、列号）
  - 编辑/预览模式
  - 树的展开状态
  - 滚动位置
- 启动时自动恢复

#### 实现思路
- 状态文件路径：`~/.vnote/session.json`
- 保存时机：每次 `ctrl+q` 退出时
- 恢复时机：`New()` 或 `Init()` 中读取
- 数据结构：
  ```go
  type SessionState struct {
      CurrentPath string          `json:"currentPath"`
      CursorRow   int             `json:"cursorRow"`
      CursorCol   int             `json:"cursorCol"`
      Mode        string          `json:"mode"`
      Expanded    map[string]bool `json:"expanded"`
      TreeOffset  int             `json:"treeOffset"`
      PreviewOff  int             `json:"previewOff"`
      ActivePane  string          `json:"activePane"`
  }
  ```
- 如果文件不存在或解析失败，静默忽略（首次启动正常）

#### 保存/恢复逻辑
- 保存：`saveSession()` — 在 `ctrl+q` 处理中调用
- 恢复：`restoreSession()` — 在 `New()` 中调用
- 写入用 `os.WriteFile(path, jsonBytes, 0o600)`
- 读取用 `os.ReadFile(path)` + `json.Unmarshal`

### 3. 全局搜索

#### 交互流程
- `Ctrl+Shift+O` (字母 O for "everywhere") 触发全局搜索
- 底部出现搜索框，输入关键词
- 实时搜索：匹配笔记标题 + 笔记内容
- 搜索结果列表显示：
  - 左侧：笔记标题（文件夹 bold）
  - 右侧：匹配内容的片段（用 muted 色 + 高亮关键词）
- `↑/↓` 选择搜索结果，`Enter` 打开该笔记并退出搜索
- `Esc` 取消搜索

#### 实现思路
- 新增 `modeSearchGlobal` mode
- 搜索逻辑：
  ```go
  type globalSearchResult struct {
      path    string
      title   string
      snippet string  // 匹配行的内容
      lineNum int
  }
  ```
- 遍历所有笔记，读取内容，查找匹配行
- 高亮：匹配文本用 accent 色 + bold
- 打开笔记后自动跳到匹配行

#### 搜索性能
- 笔记数量一般不多（几十到几百），逐文件读取即可
- 显示前 20 条结果即可
- 输入防抖：每次 keypress 后延迟 150ms 搜索

### 4. 编辑模式：更好的列表编辑体验

- 当光标在列表行末尾按 Enter：
  - 如果当前行是 `- item`，新行自动加 `- `
  - 如果当前行是 `1. item`，新行自动加 `2. `（递增）
  - 如果当前行是 `[ ] task`，新行自动加 `[ ] `
  - 空列表行（只有 `- ` 或 `1. `）按 Enter 清空该行并退出列表模式

#### 实现
- 在 `enterIndent()` 基础上扩展列表续行逻辑
- 用正则检测当前行是否是列表行
- 自动递增有序列表编号

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- `Ctrl+Shift+F` 进入专注模式，全屏编辑
- `Esc` 退出专注模式
- 退出后重启，恢复之前的笔记和光标位置
- `Ctrl+Shift+O` 全局搜索，显示匹配列表
- 搜索结果选择后打开笔记跳到匹配行
- 有序列表自动递增编号

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase7): focus mode + session persistence + global search"`
- 完成后 push 到远端
