# Phase 14 — HTML 导出 + 最近笔记 + 键盘导航打磨

## 核心目标
在保持最小、优雅、专业的前提下，加入两个高价值小特性：把笔记导出为独立 HTML 页面，以及在 Quick Open 中提供最近打开的笔记。同时打磨状态栏层级与结果列表的键盘导航。

## 不动的东西
- 配色体系（palette.go）完全保留：低饱和冷灰底 + 蓝色 accent
- 存储层保持普通 `.md` 文件，无数据库或专有格式
- 不引入 PDF 等需要外部二进制才能可靠生成的重型导出路径

## 新特性

### 1. Markdown → HTML 导出
- 导出菜单新增 `3 导出 HTML`；`Alt+H` 直接进入当前笔记的 HTML 导出路径
- 批量导出改为两步：先选 `1 Markdown` 或 `2 HTML`，再输入目标目录
- 使用 goldmark + GFM 渲染正文，并用 bluemonday 清洗原始 HTML，避免导出文件执行脚本
- 生成自包含、内嵌样式的 HTML 页面：标题、路径、创建日期、标签、正文，配色沿用冷灰 + 蓝色 accent 的浅色纸张版本
- 命令面板新增 “Export as HTML”

### 2. Quick Open 最近笔记
- 会话文件持久化最近打开的笔记（去重、按最近优先、最多 20 条）
- Quick Open 查询为空时列出最近笔记（仅保留仍存在的笔记），Enter 直接打开
- 打开、后退/前进、全局搜索/Quick Open 打开路径都会刷新最近列表

## 交互优化
- Quick Open、全局搜索、命令面板新增 `Home` / `End` / `PgUp` / `PgDn` 导航
- 帮助文本补充预览页内 `Space` 下翻、`b` 上翻（viewport 原生支持）

## UI 打磨
- 状态栏左侧快捷键与右侧状态/阅读进度之间加入弱 `│` 分隔，层级更清晰
- 顶栏品牌与页签分隔符改为 `│`，视觉更利落
- 导出路径对话框明确显示当前 `Format: Markdown / HTML`

## 验收
- `go build ./...` 通过
- `go test ./...` 通过（新增 HTML 导出、批量 HTML、清洗、最近笔记、Home/End 导航测试）
- `go vet ./...` 通过
- README 与帮助文本同步更新

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase14): HTML export + recent notes + keyboard navigation polish"`
