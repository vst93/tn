# Phase 3 — 编辑体验与快捷方式增强

## 目标
让编辑体验更流畅、更直觉，快捷键更丰富、更一致。

## 当前问题
- 编辑时没有视觉上的"编辑模式"提示（只有 header 文字变了）
- 保存后没有成功反馈
- 没有"查找"功能
- 没有"跳转到行"功能
- 没有"撤销/重做"提示
- 复制成功没有明显反馈
- 删除确认弹窗太简陋

## 不动的东西
- 配色体系（palette.go）
- 布局结构（Phase 1 三段式宽度）
- Markdown 渲染（Phase 2 已优化）
- main.go / storage 层
- 不引入新外部依赖

## 具体优化点

### 1. 编辑模式视觉强化
- 进入编辑模式时，整个内容区边框从 rule 色变为 accent 色（已有，确认生效）
- 编辑模式下 header 右侧显示 `EDIT` 标签（加粗 + accent 背景）
- 编辑模式下 footer 显示 `Ctrl+S 保存 · Esc 预览` 提示

### 2. 保存反馈
- 保存成功时，status 行短暂显示绿色 `✓ Saved` 消息（2 秒后自动消失）
- 保存失败时显示红色错误消息

### 3. 复制反馈
- 复制成功后，status 行短暂显示 `✓ Copied to clipboard`（1.5 秒后消失）
- 复制失败显示错误

### 4. 删除确认弹窗增强
- 弹窗显示要删除的文件名
- 显示 `Enter/Y 确认删除` 和 `Esc/N 取消`
- 危险操作用 danger 色高亮

### 5. 新建笔记后自动进入编辑模式
- 创建新笔记后自动切换到编辑模式
- 光标自动定位到编辑器

### 6. 切换面板时保持状态
- 从 tree 切回 content 时，如果之前在编辑模式，保持编辑模式
- 从 content 切回 tree 时，如果已保存，保持预览模式

### 7. 状态消息自动消失
- 成功/错误消息在 2-3 秒后自动清除
- 使用 tea.Tick 或类似机制

### 8. 快捷键提示增强
- 在 footer 的快捷键提示中，编辑模式下显示不同的快捷键集
- 编辑模式：`Ctrl+S 保存 · Esc 退出编辑`
- 普通模式：保持现有

## 验收
- `go build ./...` 通过
- `go test ./...` 通过
- `go vet ./...` 通过
- 编辑模式有清晰的视觉区分
- 保存/复制后有成功反馈
- 新建笔记后自动进入编辑模式
- 状态消息自动消失

## 构建环境
- `GOPROXY=https://goproxy.cn,direct`
- `go build ./...` / `go test ./...` / `go vet ./...`

## 提交
- `git add -A && git commit -m "feat(phase3): editing experience polish — save/copy feedback, auto-edit, status timeout"`
- 不要 push
